const Projects = () => {
  const projects = {};
  return (
    <section
      id="projects"
      className="h-full w-full flex justify-center p-8 lg:p-0"
    >
      <div className="md:max-w-[360px] lg:max-w-[1080px] w-full flex flex-col lg:flex-row justify-center items-center gap-16">
        <h1 className="text-white font-bold text-4xl">Proyectos</h1>
      </div>
    </section>
  );
};

export default Projects;
